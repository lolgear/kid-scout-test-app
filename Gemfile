ruby '2.4.1'
source 'https://rubygems.org'


gem 'rails', '~> 5.2'

# database
gem 'sqlite3', '~> 1.3.6'

# json serialization
gem 'active_model_serializers'

# add cancancan gem for auto load resources or 'load_and_authorize_resource' gem.

# bootsnap
gem 'bootsnap', require: false

group :development, :test do
  gem 'factory_bot_rails'
  gem 'rspec-rails'
  gem 'rspec-its'
  gem 'shoulda-matchers'
  gem 'rails-controller-testing'
  # gem 'guard'
  # gem 'guard-rspec'
  # gem 'guard-bundler'
  # # terminal guard notifier
  # gem 'terminal-notifier-guard', '~>1.6.1'
  gem 'listen'
  gem 'ruby-prof'
end
